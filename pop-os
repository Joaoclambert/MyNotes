# Executar arquivo .deb 
sudo dpkg -i nome arquivo
----------------------------
# Instalando e configurando asdf

1. Clone asdf
git clone https://github.com/asdf-vm/asdf.git ~/.asdf --branch v0.12.0

2. Informar caminho ao bash ou zsh
echo ". $HOME/.asdf/asdf.sh" >> .bashrc
echo ". $HOME/.asdf/completions/asdf.bash" >> .bashrc
echo ". $HOME/.asdf/asdf.sh" >> .zshrc

3. Reiniciar o terminal
----------------------------
# Instalando java com asdf:
1. Adicionar plugin java ao asdf
asdf plugin-add java https://github.com/halcyon/asdf-java.git
2. Encontrar versao desejada 
asdf list-all java | grep oracle  
3. Instalar a versão escolhida
asdf install java $VERSION (ex: oracle-20.0-1)
4.Definir a versao global
asdf global java $VERSION (ex: oracle-20.0.1) 

